# Legacy Agent Compatibility Entry Point

This filename is retained temporarily for compatibility. No repository script, workflow, or configuration was found to consume it by exact name during the 2026-08-22 context migration.

Read and follow `AGENTS.md`, then consult the task-relevant files under `docs/`.

Do not treat `.context/` as canonical; it is retained only for migration review.
