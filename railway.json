{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "npx expo start --dev-client --tunnel --port $PORT",
    "restartPolicyType": "ALWAYS"
  }
}
